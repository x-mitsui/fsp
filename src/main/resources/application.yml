#??????
server:
  port: 8080
#?????
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/family_service_platform?serverTimezone=UTC&useSSL=false
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
#??mybatis
mybatis:
  mapper-locations: classpath:com/dzq/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
#sql??????
logging:
  level:
    com:
      dzq:
        mapper: debug
